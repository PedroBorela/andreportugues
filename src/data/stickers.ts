import type { Sticker } from '../lib/types';

/** CONFIRMAR: pacote de figurinhas do comitê. Com a lista vazia, o bloco de figurinhas não é exibido. */
export const stickers: Sticker[] = [];

/** Quantidade de figurinhas exibidas na grade da página (o restante fica no modal). */
export const stickerPreviewCount = 16;
